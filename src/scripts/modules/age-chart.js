import * as d3 from 'd3';

export default (function() {
  let config = {
    container: '.age-chart',
    tooltip: '.age-chart-tooltip',
    width: 960,
    height: 820,
    midX: 480
  };

  function init() {

  }

  return {
    init
  };
})();
